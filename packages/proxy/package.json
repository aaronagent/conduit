{
  "name": "@conduit/proxy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "hono": "^4.9.9"
  },
  "devDependencies": {
    "vitest": "^4.1.0"
  }
}
